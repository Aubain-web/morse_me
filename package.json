{
  "name": "morse-me",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "morse_it": "cli.mjs"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node --test dist/tests"
  },
  "files": [
    "cli.mjs",
    "dist"
  ],
  "dependencies": {
    "pcm-player": "^0.0.18",
    "yargs": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/yargs": "^17.0.35",
    "typescript": "^5.3.3"
  }
}
